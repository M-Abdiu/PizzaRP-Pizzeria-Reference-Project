name = input ("Bitte Namen eingeben: ")
while (name.strip().lower() != "stop")      # strip nimmt die leerzeichen weg. #lower macht kleinbuchstaben raus
    if not name:                            #leer
        print("Sie haben keinen Namen eingegeben")
    else: #weiter machen mit Noten eingabe usw. 
        
